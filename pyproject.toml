[project]
name = "geogeeks"
version = "0.1.0"
description = "geogeeks documentation site (MkDocs Material)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-awesome-pages-plugin>=2.10.1",
    "mkdocs-material>=9.7.7",
]
